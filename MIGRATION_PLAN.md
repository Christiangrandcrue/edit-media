# 🔄 План миграции EDIT к проектной архитектуре

## 📋 Этапы миграции

### **Этап 1: Подготовка БД и API**
**Оценка:** 2-3 часа

1. **Создать новые таблицы:**
   - `projects`
   - `project_assets`
   - `master_videos`
   - `scaled_videos`

2. **Миграция существующих данных:**
   - Создать проект "Legacy" 
   - Перенести shots → project_assets
   - Сохранить старые jobs как master_videos

3. **Новые API endpoints:**
   - `/api/projects/*`
   - `/api/projects/:id/assets/*`
   - `/api/projects/:id/master-videos/*`

**Результат:** БД готова, старые данные сохранены

---

### **Этап 2: Рефакторинг backend**
**Оценка:** 3-4 часа

1. **Создать сервисы:**
   - `project-service.js` — управление проектами
   - `asset-service.js` — загрузка материалов
   - `master-video-service.js` — создание мастер-роликов

2. **Адаптировать существующие сервисы:**
   - `generator.js` → работа с project_id
   - `shot-service.js` → работа с project_assets
   - Добавить metadata в `video_*_metadata.json`

3. **Интеграция скриптов:**
   - `extract_and_merge_audio.cjs` → API endpoint
   - `merge_video_audio.cjs` → API endpoint
   - Автоматический вызов после генерации

**Результат:** Backend работает с проектами

---

### **Этап 3: Новый UI (MVP)**
**Оценка:** 4-5 часов

1. **Страница списка проектов:**
   - `/projects` — список всех проектов
   - Карточки проектов с статусом

2. **Страница создания проекта:**
   - `/projects/new` — форма создания
   - Загрузка шотов (hook/mid/cta)
   - Загрузка целевых роликов

3. **Страница проекта:**
   - `/projects/:id` — dashboard проекта
   - Табы: Assets, Master Videos, Scale

4. **Адаптировать существующий UI:**
   - Редактор синхронизации → привязка к master_video_id
   - Admin panel → работа с project_assets

**Результат:** Базовый UI для проектов

---

### **Этап 4: Создание мастер-роликов**
**Оценка:** 3-4 часа

1. **Из шотов:**
   - Форма выбора шотов
   - API создания master_video
   - Автоматическая генерация (video → audio → merge)

2. **Интеграция редактора синхронизации:**
   - Загрузка master_video в редактор
   - Сохранение маркеров в БД
   - Кнопка "Одобрить как мастер"

3. **Статусы обработки:**
   - Создание → Генерация → Извлечение аудио → Наложение → Готов
   - Live progress через WebSocket

**Результат:** Мастер-ролики создаются и проверяются

---

### **Этап 5: Масштабирование**
**Оценка:** 2-3 часа

1. **Мешап (существующая логика):**
   - Адаптировать под project_id
   - Использовать approved master_videos
   - Сохранять в scaled_videos

2. **Batch управление:**
   - Создание batch
   - Мониторинг прогресса
   - Список готовых видео

3. **Экспорт:**
   - Архив всего batch
   - Скачивание отдельных видео

**Результат:** Масштабирование работает

---

### **Этап 6: Дополнительные функции**
**Оценка:** 4-6 часов (опционально)

1. **Нарезка целевого ролика:**
   - Видеоплеер с таймлайном
   - Отметки для нарезки
   - FFmpeg резка по меткам

2. **Уникализация:**
   - Применение фильтров к мастер-ролику
   - Вариации (цвет, скорость, шум)
   - Сохранение в scaled_videos

3. **Улучшения UI:**
   - Drag & drop загрузка
   - Превью видео
   - Batch операции

**Результат:** Полный функционал

---

## 🚀 Быстрый старт (MVP за 1 день)

### **Приоритет 1: Базовая инфраструктура (3 часа)**
1. Создать новые таблицы БД
2. Создать `/api/projects` endpoints
3. Миграция существующих shots в project_assets

### **Приоритет 2: Создание мастер-роликов (4 часа)**
1. Страница `/projects/:id/master-videos/new`
2. API создания мастер-ролика из шотов
3. Интеграция генератора → аудио → merge
4. Привязка редактора синхронизации

### **Приоритет 3: Масштабирование (2 часа)**
1. Адаптировать существующий генератор
2. Страница `/projects/:id/scale`
3. Batch управление

**Итого:** ~9 часов на MVP

---

## 📦 Обратная совместимость

Во время миграции сохраним старые endpoints:
- `/api/jobs` → работает как раньше
- `/api/shots` → работает как раньше
- `/admin` → работает как раньше

После миграции:
- Старые endpoints → deprecated
- Перенаправление на новые
- Удаление через 1-2 недели

---

## ✅ Критерии успеха

**MVP готов когда:**
1. ✅ Можно создать проект
2. ✅ Можно загрузить шоты
3. ✅ Можно создать мастер-ролик из шотов
4. ✅ Можно проверить синхронизацию
5. ✅ Можно запустить масштабирование (мешап)
6. ✅ Можно скачать готовые видео

**Полная версия готова когда:**
1. ✅ Все функции MVP
2. ✅ Нарезка целевого ролика
3. ✅ Уникализация мастер-роликов
4. ✅ Улучшенный UI/UX
5. ✅ Документация

---

## 🎯 Следующие шаги

**Вариант А: Полная миграция (~2 дня)**
- Реализуем все этапы 1-5
- Получаем production-ready систему

**Вариант Б: MVP сейчас, остальное потом (~1 день)**
- Реализуем только приоритеты 1-3
- Остальное добавляем по мере необходимости

**Вариант В: Только концепция**
- Сохраняем документацию
- Реализуем когда потребуется

---

**Какой вариант выбираешь?**
