# 🔄 ОТСЛЕЖИВАНИЕ ПРОГРЕССА MASTER-РОЛИКОВ

**Дата**: 2026-01-18  
**Реализовано**: ~15 минут  
**Статус**: ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ

---

## 🎯 ЧТО ДОБАВЛЕНО

### 1. **Автоматическое обновление статусов (Polling)**
- Каждые **5 секунд** проверяется статус master-роликов
- Работает для статусов: `pending` и `processing`
- Автоматически останавливается при: `completed` или `failed`
- Timeout: 30 минут (потом автостоп)

### 2. **Timestamp последнего обновления**
```
📹 002
   Материалы: 3
   🕐 обновлено 2 мин. назад  ← НОВОЕ!
```

### 3. **Уведомления о готовности**
- ✅ "Master-ролик готов!" (зелёное) — когда статус `completed`
- ❌ "Ошибка обработки" (красное) — когда статус `failed`

### 4. **Консольные логи для отладки**
```javascript
🔄 Started polling for master_xxx
✅ Polling stopped for master_xxx: completed
⏱️ Polling timeout for master_xxx
```

---

## 🔍 КАК ЭТО РАБОТАЕТ

### **Сценарий 1: Создание master-ролика**
1. Выбрать материалы → Создать master-ролик
2. Статус: `created` (синий) → `pending` (жёлтый)
3. **Polling автоматически запускается**
4. Каждые 5 секунд обновляется timestamp: "обновлено только что"
5. Когда backend меняет статус на `processing` → спиннер появляется
6. Когда статус → `completed` → уведомление + перезагрузка проекта

### **Сценарий 2: Отслеживание прогресса**
1. Открыть проект с master-роликом в статусе `pending`/`processing`
2. Видеть timestamp: "обновлено 30 сек. назад"
3. **Timestamp обновляется каждые 5 секунд**
4. Если статус не меняется → значит backend ещё обрабатывает
5. Если timestamp застыл → возможно backend завис (нужно проверить логи)

### **Сценарий 3: Понять что зависло**
**Если timestamp НЕ обновляется:**
- Проблема в frontend (polling не работает) → проверить консоль браузера

**Если timestamp обновляется, но статус не меняется:**
- Backend обрабатывает (нормально)
- ИЛИ backend завис (проверить логи backend)

**Если прошло >5 минут без изменений:**
- Вероятно backend завис → проверить:
  ```bash
  pm2 logs backend --nostream
  ```

---

## 🛠️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### **API Polling**
```javascript
// Каждые 5 секунд
GET /api/projects/{projectId}/master-videos/{masterId}

// Ответ:
{
  "success": true,
  "master_video": {
    "master_id": "master_xxx",
    "status": "processing",  // или completed/failed
    "updated_at": "2026-01-18T20:00:00.000Z"
  }
}
```

### **Функции**
- `startPolling(projectId, masterId)` — запуск polling
- `getTimeAgo(date)` — форматирование времени ("2 мин. назад")
- `activePolling` — Set активных polling задач

### **Lifecycle**
```
created → polling НЕ запускается (ждёт backend)
pending → polling ЗАПУСКАЕТСЯ
processing → polling продолжается + спиннер
completed → polling ОСТАНАВЛИВАЕТСЯ + уведомление
failed → polling ОСТАНАВЛИВАЕТСЯ + ошибка
```

---

## ✅ ИНДИКАТОРЫ РАБОТОСПОСОБНОСТИ

### **Polling работает если:**
✅ Timestamp обновляется каждые 5 секунд  
✅ В консоли: `🔄 Started polling for master_xxx`  
✅ При изменении статуса: уведомление + перезагрузка

### **Polling НЕ работает если:**
❌ Timestamp не обновляется  
❌ Нет логов в консоли  
❌ Статус изменился, но проект не обновился

---

## 🧪 ТЕСТИРОВАНИЕ

### **Шаг 1: Создать master-ролик**
```
1. Открыть: https://edit.synthnova.me/dashboard
2. Проект "Василий1" → выбрать материалы
3. "Создать master-ролик" → ввести название → Создать
4. Статус: "Создан" (синий)
```

### **Шаг 2: Проверить polling**
```
1. Открыть консоль браузера (F12)
2. Искать: "🔄 Started polling for master_xxx"
3. Каждые 5 секунд: новый API запрос
4. Timestamp обновляется: "обновлено только что"
```

### **Шаг 3: Дождаться готовности**
```
1. Статус меняется: pending → processing → completed
2. Уведомление: "✅ Master-ролик готов!"
3. Проект автоматически обновляется
4. Кнопка "Просмотр" становится активной
```

---

## 📊 ОЖИДАЕМОЕ ПОВЕДЕНИЕ

| Статус | Polling | Timestamp | Спиннер | Действия |
|--------|---------|-----------|---------|----------|
| `created` | ❌ | статичный | ❌ | "Ожидание" |
| `pending` | ✅ | обновляется | ❌ | "Ожидание" |
| `processing` | ✅ | обновляется | ✅ | "Обработка..." |
| `completed` | ❌ | финальный | ❌ | "Просмотр", "Скачать" |
| `failed` | ❌ | финальный | ❌ | "Повторить" |

---

## 🐛 ОТЛАДКА

### **Проверка polling в консоли**
```javascript
// Список активных polling задач
console.log(activePolling)

// Ручной запуск polling (для тестирования)
startPolling('project_xxx', 'master_xxx')
```

### **Проверка API вручную**
```bash
curl 'https://edit.synthnova.me/api/projects/project_xxx/master-videos/master_xxx' | jq '.master_video.status'
```

---

## 📦 ОБНОВЛЁННЫЕ ФАЙЛЫ

**На сервере** (185.178.46.187):
- `/home/synthnova/frontend/projects-dashboard.html` (+3 KB)
  - Функция `startPolling()`
  - Функция `getTimeAgo()`
  - Timestamp в карточках master-роликов
  - Автозапуск polling для pending/processing

---

## 🎯 ИТОГ

### ✅ **Реализовано:**
- Polling каждые 5 секунд
- Timestamp последнего обновления
- Автоуведомления о готовности
- Автостоп polling при завершении
- Консольные логи для отладки

### 🎁 **Бонус:**
- Теперь понятно: работает ли обработка или зависла
- Timestamp показывает "свежесть" данных
- Не нужно вручную обновлять страницу

---

## 🚀 ПРОТЕСТИРУЙТЕ СЕЙЧАС

**URL**: https://edit.synthnova.me/dashboard

**Что делать:**
1. Откройте проект "Василий1"
2. Найдите master-ролик со статусом "Создан"/"Ожидание"
3. Откройте консоль браузера (F12)
4. Ждите 5 секунд → увидите обновление timestamp
5. Смотрите логи: "🔄 Started polling..."

**Если всё работает:**
- Timestamp обновляется каждые 5 секунд
- Логи появляются в консоли
- При готовности — уведомление

---

**Автор**: Claude  
**Контакт**: Christian
